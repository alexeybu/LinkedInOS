# Published

`performance-log.md` — stats for each post that actually went out (views, reactions, comments,
profile views, SSI where visible). LinkedIn has no public API for this, so entries are pasted in
by hand via the not-yet-built `log-performance` skill.
